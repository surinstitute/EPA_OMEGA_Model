# This requirements file is for Read the Docs builds only.
# Runtime dependencies for the model are managed in pyproject.toml.

# Keep Sphinx pinned for Python 3.7 compatibility used in .readthedocs.yaml
Sphinx<4.0.0
jinja2==2.11.3
markupsafe==2.0.1

# RTD theme + historical pin used to stabilize RTD builds
sphinx-rtd-theme>=1.0.0,<=2.0.0
requests==2.29.0
